import ScaffoldStub from "@/components/ScaffoldStub";
export default function Page() {
  return <ScaffoldStub name="Showcase" note="The creator's public showcase (grid / editorial / minimal). To be ported." />;
}
