import ScaffoldStub from "@/components/ScaffoldStub";
export default function Page() {
  return <ScaffoldStub name="Add product" note="Source URL → fetch → auto-fill → set markup. To be ported next." />;
}
